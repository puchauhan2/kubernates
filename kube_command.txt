####### pod common command ######

kubectl get deployments -A
kubectl get replicasets -A
kubectl get pods -A
kubectl get pods --show-labels
kubectl describe pod <pod-name>

####### Network Command ######

kubectl get endpoints
kubectl get endpointslices
kubectl get endpoints <Service_name>


kubectl get ns = kubectl get namespace
kubectl config view --minify | grep namespace
kubectl config set-context --current --namespace=wordpress # set default namespace

kubectl get configmap
kubectl describe configmap wordpress-config

kubectl get secret

kubectl rollout restart deployment wordpress ## after modifying config or secreat file we have to run this command to load new value
OR
Re-run deployment : kubectl apply -f wordpress-deployment.yaml ## Kubernetes creates new Pods, and those new Pods read the latest ConfigMap.

MySQL
   │
   ├── Stable Network Identity
   ├── Persistent Storage
   ├── Stable Pod Name
   └── Stable DNS

kubectl apply -f mysql-pvc.yaml
kubectl describe pvc mysql-pvc
kubectl get pv
kubectl describe pvc mysql-pvc


kubectl delete pod <one-wordpress-pod-name> -n wordpress  ## delete pod under given namespace

kubectl scale statefulset mysql --replicas=2 -n wordpress ## increasing replica

kubectl get pods --show-labels -n wordpress # show label under namespace

kubectl describe svc wordpress-service -n wordpress

kubectl get all

kubectl get ingress

helm list

helm install myblog . --namespace wordpress  ##  mention namespace all time 

kubectl get endpoints -n ingress-nginx

helm list -A

############ Helm ###############


helm template wordpress .  ## dry run, disply template after replacing value


Jogibaba#@2023

########################################
You can retrieve the previous container's logs

For example:

kubectl logs wordpress-abc --previous

The --previous flag shows the logs from the last terminated container in that Pod.

This is extremely useful when the current container has already restarted.
#####################################################################################################
puneet@localhost:~$ helm install monitoring prometheus-community/kube-prometheus-stack   -n monitoring
NAME: monitoring
LAST DEPLOYED: Fri Jul  3 16:43:26 2026
NAMESPACE: monitoring
STATUS: deployed
REVISION: 1
DESCRIPTION: Install complete
TEST SUITE: None
NOTES:
kube-prometheus-stack has been installed. Check its status by running:
  kubectl --namespace monitoring get pods -l "release=monitoring"

Get Grafana 'admin' user password by running:

  kubectl --namespace monitoring get secrets monitoring-grafana -o jsonpath="{.data.admin-password}" | base64 -d ; echo

Access Grafana local instance:

  export POD_NAME=$(kubectl --namespace monitoring get pod -l "app.kubernetes.io/name=grafana,app.kubernetes.io/instance=monitoring" -oname)
  kubectl --namespace monitoring port-forward $POD_NAME 3000

Get your grafana admin user password by running:

  kubectl get secret --namespace monitoring -l app.kubernetes.io/component=admin-secret -o jsonpath="{.items[0].data.admin-password}" | base64 --decode ; echo


Visit https://github.com/prometheus-operator/kube-prometheus for instructions on how to create & configure Alertmanager and Prometheus instances using the Operator

kubectl port-forward svc/monitoring-grafana 4000:80 -n monitoring ## port forwarding

kubectl exec -it myblog-mysql-0 -n wordpress -- /bin/bash  ## login to pod /bin/bash

kubectl get pods -A -o wide ## print all pod with IP


32ciR$i#89vef#$mvekLKesjyh
